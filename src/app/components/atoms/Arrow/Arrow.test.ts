import storySnaps from 'test/utils/storySnaps'
import * as stories from './Arrow.stories'

describe('app/components/atoms/Arrow', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
