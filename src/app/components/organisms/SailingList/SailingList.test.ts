import storySnaps from 'test/utils/storySnaps'
import * as stories from './SailingList.stories'

describe('app/components/organisms/SailingList', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
