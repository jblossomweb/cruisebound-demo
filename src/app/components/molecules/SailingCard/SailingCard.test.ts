import storySnaps from 'test/utils/storySnaps'
import * as stories from './SailingCard.stories'

describe('app/components/molecules/SailingCard', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
