import storySnaps from 'test/utils/storySnaps'
import * as stories from './SailingFilters.stories'

describe('app/components/molecules/SailingFilters', () => {
  describe('stories', () => {
    storySnaps(stories)
  })
})
